services:
  backend:
    build: .
    container_name: finance-app
    ports:
      - "8000:8000"
    depends_on:
      - redis
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379

  redis:
    image: redislabs/rejson:latest
    container_name: redis-json
    ports:
      - "6379:6379"